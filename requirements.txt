# Python dependencies (Python 3.x)
#   pip install -r requirements.txt
#
# System dependency (NOT installed by pip): ffmpeg + ffprobe must be on PATH.
#   macOS:         brew install ffmpeg
#   Debian/Ubuntu: sudo apt install ffmpeg
#
# Note: hl_ocr.py needs NO OCR engine — it detects goals by image change, not text.

Pillow>=9.0
